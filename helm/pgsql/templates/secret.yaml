apiVersion: v1
kind: Secret
metadata:
  name: "{{ .Values.app.name }}-secret"
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
{{- range $key, $value := .Values.secret.stringData }}
  {{ $key }}: {{ $value | quote }}
{{- end }}